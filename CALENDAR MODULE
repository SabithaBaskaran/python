import calendar

def get_day_name(month, day, year):

    day_index = calendar.weekday(year, month, day)
    
    
    return list(calendar.day_name)[day_index].upper()

if __name__ == '__main__':

    m, d, y = map(int, input().split())
    
    print(get_day_name(m, d, y))
