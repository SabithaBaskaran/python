
maxdepth = 0
def depth(elem, level):
    global maxdepth
    # Update maxdepth if current level is higher
    if level == maxdepth:
        maxdepth += 1
        
    # Recursively check children, incrementing the level
    for child in elem:
        depth(child, level + 1)




