if __name__ == '__main__':
    k = int(input())
    rooms = list(map(int, input().split()))
    
    unique_rooms = set(rooms)
    
    sum_unique = sum(unique_rooms)
    sum_total = sum(rooms)
    
    captains_room = (sum_unique * k - sum_total) // (k - 1)
    print(captains_room)
