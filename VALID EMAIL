def fun(s):
    import re


    pattern = r'^[a-zA-Z0-9_-]+@[a-zA-Z0-9]+\.[a-zA-Z]{1,3}$'
    
    # re.match returns a match object if valid, which evaluates to True
    return bool(re.match(pattern, s))

