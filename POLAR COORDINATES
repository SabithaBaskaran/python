import cmath

# Convert the input string directly to a complex number
z = complex(input())

# cmath.polar(z) returns a tuple (r, phi)
r, phi = cmath.polar(z)

# Output the results
print(r)
print(phi)
